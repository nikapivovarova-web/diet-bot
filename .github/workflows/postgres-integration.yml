name: integration-postgres

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: postgres-integration-deferred
    runs-on: ubuntu-latest
    steps:
      - name: PostgreSQL integration deferred
        run: echo "PostgreSQL integration is intentionally deferred; no database service or integration tests run in PR2."
